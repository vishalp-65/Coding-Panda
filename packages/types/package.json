{
  "name": "@ai-platform/types",
  "version": "1.0.0",
  "description": "TypeScript type definitions for AI Platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.1.0"
  }
}
