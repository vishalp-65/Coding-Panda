// Basic types for gRPC services without generated protobuf files
// This allows the services to compile while we set up the protobuf generation

export interface UserServiceClient {
    // Placeholder for generated client
    close(): void;
}

export interface ProblemServiceClient {
    // Placeholder for generated client
    close(): void;
}

export interface ExecutionServiceClient {
    // Placeholder for generated client
    close(): void;
}

export interface ContestServiceClient {
    // Placeholder for generated client
    close(): void;
}

export interface AnalyticsServiceClient {
    // Placeholder for generated client
    close(): void;
}

export interface NotificationServiceClient {
    // Placeholder for generated client
    close(): void;
}

export interface AIAnalysisServiceClient {
    // Placeholder for generated client
    close(): void;
}

// Service definitions (placeholders)
export const UserServiceService = {};
export const ProblemServiceService = {};
export const ExecutionServiceService = {};
export const ContestServiceService = {};
export const AnalyticsServiceService = {};
export const NotificationServiceService = {};
export const AIAnalysisServiceService = {};